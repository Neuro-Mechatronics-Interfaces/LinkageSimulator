\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{float}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,positioning}
\usepackage[hidelinks]{hyperref}

\title{Kinematic and Constraint Basis of the Linkage Simulator}
\author{Implementation-derived technical note}
\date{}

\begin{document}
\maketitle

\section{Scope and conventions}

The application is a deterministic planar kinematics simulator with a bounded
quasi-static gravity estimate, not a dynamic, clinical, or musculoskeletal
model. The serial finger abstraction follows established hand kinematic
modelling practice \cite{an1979normative,buchholz1992anthropometric}. World
coordinates use millimetre-like units, angles use radians internally, and
positive angles are counter-clockwise. A rigid link has pose
$(\mathbf{p},\theta)$, and an arbitrary local point $\mathbf{q}$ maps to world
space as
\begin{equation}
  \mathbf{x}(\mathbf{p},\theta,\mathbf{q})
  =\mathbf{p}+R(\theta)\mathbf{q}, \qquad
  R(\theta)=
  \begin{bmatrix}
    \cos\theta&-\sin\theta\\
    \sin\theta& \cos\theta
  \end{bmatrix}.
\end{equation}
The rectangular length and width are rendering and editing dimensions; joint
attachments are not required to lie at $\pm L/2$. Geometry and mechanism
solving have no DOM or Canvas dependency, and rendering does not mutate state.

\section{Independent digit workspaces}

The application instantiates independent simulation states for D2 through D5
and renders only the active tab. Servo angle, animation direction, linkage
edits, contactors, joint-limit status, and static-moment results therefore
persist independently for each digit. A shared hand-size factor $h$ applies to
all four anatomical models. Neutral initialization uses rounded between-digit
scales
\begin{equation}
  (s_2,s_3,s_4,s_5)=(1.00,1.11,1.03,0.83),
\end{equation}
consistent with reported young-adult mean digit lengths of approximately
$63.9$, $70.7$, $65.5$, and $53.3\,\mathrm{mm}$, respectively
\cite{chatzioglou2024biometric}. For base D2 segment length $L_{i,2}$, digit
$d$ is initialized with $L_{i,d}=h s_d L_{i,2}$. The linkage template is scaled
by $s_d$ at creation; subsequent hardware edits remain local to one digit.

\section{Current default dorsal mounting and actuator convention}

The current demonstrator uses one fixed dorsal rail; it is distinct from the
three-point synthesis base introduced in Section~\ref{sec:reference-synthesis}.
Let $\mathbf{G}$ be a point on the dorsal ground plane, with tangent
$\mathbf{t}_g$ and dorsal normal $\mathbf{n}_g$. The editable servo--ground
offset $h_g$ places the servo axis at
\begin{equation}
  \mathbf{O}_s=\mathbf{G}+h_g\mathbf{n}_g.
\end{equation}
The fixed dorsal rail has length $L_g$ and stored orientation $\alpha_g$, so its
second pivot is
\begin{equation}
  \mathbf{D}=\mathbf{O}_s+L_g(\cos\alpha_g,\sin\alpha_g).
\end{equation}
The rail is identified through the servo's referenced revolute joint rather
than a reserved link name. That revolute must connect the driven link to world
or a fixed link, and its fixed-side world attachment must coincide with
$\mathbf{O}_s$; unsupported or mismatched mounts are rejected during graph
construction.

The primary servo command $\phi$ preserves the original application semantics:
it is the absolute world orientation of the driven link, not an angle relative
to the rail. If $\mathbf{q}_s$ is the driven link's local point at the servo
revolute, the actuator seed is
\begin{equation}
  \theta_s=\phi, \qquad
  \mathbf{p}_s=\mathbf{O}_s-R(\phi)\mathbf{q}_s.
\end{equation}
The animation advances $\phi$ between its configured limits and reverses at
each bound. Actuator mobility analysis adds the equality
$\operatorname{wrap}(\theta_s-\phi)=0$; passive analysis omits it.

\section{Constraint graph and connected components}

Every rigid link is a graph body, together with a world body. A fixed link is
connected to world and contributes no configuration variables. Each revolute
joint is an edge between two link bodies or between a link and world. A
zero-range revolute adds a locked relative-angle edge, and the servo contributes
an actuator edge referencing its driven revolute joint. A linear-slot edge joins
a pin body to a slot body (or world) and contributes the single normal-closure
equality derived below. Component discovery is based on adjacency and is
independent of IDs and array insertion order.

For each connected component the analysis records its links, joints,
actuators, connection to fixed/world geometry, unresolved links, scalar
residual count, Jacobian ranks, and local mobilities. A component without a
world or fixed connection is reported as free-floating. Its three global rigid
motions are retained; it is never silently attached to world. Multiple
components are analyzed separately.

Figure~\ref{fig:solver-flow} shows the update path. Analytic propagation remains
the preferred solver. Numerical minimization is reached only for an unresolved
subsystem that is still constrained rather than genuinely free.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,height=0.34\textheight,keepaspectratio]
    {figures/constraint-solver-flow.pdf}
  \caption{Constraint-graph analysis, mechanism solution, and downstream
  finger/contact processing.}
  \label{fig:solver-flow}
\end{figure}

\section{Reference-derived single-digit synthesis architecture}
\label{sec:reference-synthesis}

The reference configuration is not a conventional four-bar. The servo axis
$O_s$ and two distinct dorsal anchors $G_1$ and $G_2$ belong to one rigid base
$\mathcal G$. An eccentric horn pin $E$ drives one common ternary bar
$\mathcal C$. Two separately grounded followers, $G_1B_1$ and $G_2B_2$, meet
that same bar at different material offsets $B_1\ne B_2$. The sole distal
contact $P_c$ is a fixed point of $\mathcal C$, not the end of another hinged
output link. Figure~\ref{fig:digit-synthesis} follows this incidence; its
coordinates are illustrative because perspective and occlusion do not support
dimension extraction from the photographs.

Using base-fixed offset vectors makes the three distinct attachment sites
explicit:
\begin{equation}
  \mathbf G_i=\mathbf O_s+\mathbf b_i,\qquad
  \mathbf E(\phi_k)=\mathbf O_s+R(\phi_k)\mathbf c_s,\qquad
  \|\mathbf c_s\|=r_s,\qquad i\in\{1,2\}.
  \label{eq:reference-base}
\end{equation}
If $\mathbf u_{\mathcal C}$ is the ternary bar's local straight-span axis,
the two follower attachments and sole rigid contact are parameterized by
\begin{align}
  \mathbf B_1&=\mathbf E+R(\theta_{\mathcal C})
    (\sigma_1\mathbf u_{\mathcal C}),&
  \mathbf B_2&=\mathbf E+R(\theta_{\mathcal C})
    (s_{2,k}\mathbf u_{\mathcal C}),\label{eq:reference-bar-offsets}\\
  \mathbf P_c&=\mathbf B_1+R(\theta_{\mathcal C})\mathbf p_c,&
  \mathbf p_c&=\rho_c(\cos\psi_c,\sin\psi_c).
\end{align}
Thus $B_1$ is the fixed far-end attachment, while $B_2$ occupies a distinct
bounded coordinate on the same driven bar.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      x=0.78cm,y=0.78cm,
      joint/.style={circle,draw=black,fill=white,inner sep=0pt,minimum size=5.5pt,line width=0.8pt},
      base/.style={draw=black!65,fill=black!8,line width=1.2pt},
      rocker/.style={line width=3.2pt,draw=black!72,line cap=round},
      driven/.style={line width=3.5pt,draw=blue!68!black,line cap=round},
      coupler/.style={line width=4.2pt,draw=teal!70!black,line cap=round,line join=round},
      finger/.style={line width=8pt,draw=black!17,line cap=round},
      dim/.style={{Latex[length=2mm]}-{Latex[length=2mm]},thin,draw=black!70},
      callout/.style={font=\scriptsize,fill=white,inner sep=1.5pt},
      box/.style={draw=black!35,rounded corners=2pt,fill=black!2,align=left,
                  font=\scriptsize,text width=4.65cm,inner sep=5pt}
    ]
    % Simplified digit geometry.
    \coordinate (TIP) at (0.35,0.15);
    \coordinate (DIP) at (2.50,0.02);
    \coordinate (PIP) at (4.75,0.04);
    \coordinate (MCP) at (7.15,0.22);
    \draw[finger] (TIP)--(DIP)--(PIP)--(MCP);
    \foreach \p/\name in {TIP/tip,DIP/DIP,PIP/PIP,MCP/MCP}
      \node[joint,minimum size=4.5pt] at (\p) {};
    \node[callout,below=4pt] at (TIP) {tip};
    \node[callout,below=4pt] at (DIP) {DIP};
    \node[callout,below=4pt] at (PIP) {PIP};
    \node[callout,below=4pt] at (MCP) {MCP};
    \node[callout,above=5pt] at ($(TIP)!0.5!(DIP)$) {$\ell_d$};
    \node[callout,above=5pt] at ($(DIP)!0.5!(PIP)$) {$\ell_m$};
    \node[callout,above=5pt] at ($(PIP)!0.5!(MCP)$) {$\ell_p$};

    % Rigid dorsal base, eccentric servo crank, and one common ternary bar.
    \coordinate (OS) at (9.10,3.10);
    \coordinate (GONE) at (8.55,5.20);
    \coordinate (GTWO) at (7.35,4.60);
    \coordinate (E) at (8.15,3.55);
    \coordinate (BONE) at (1.75,1.82);
    \coordinate (PC) at (1.15,0.65);
    \coordinate (BTWO) at ($(E)!0.28!(BONE)$);
    \draw[base] (OS)--(GONE)--(GTWO)--cycle;
    \node[callout] at (8.35,4.36) {$\mathcal G$};
    \draw[rocker] (GONE)--(BONE);
    \draw[rocker] (GTWO)--(BTWO);
    \draw[driven] (OS)--(E);
    \draw[coupler] (E)--(BONE)--(PC);

    % One ordinary follower joint and one bounded slot on the same bar.
    \draw[line width=7pt,draw=orange!25,line cap=round]
      ($(E)!0.21!(BONE)$)--($(E)!0.35!(BONE)$);
    \draw[line width=1.2pt,draw=orange!80!black,line cap=round]
      ($(E)!0.21!(BONE)$)--($(E)!0.35!(BONE)$);

    \foreach \p/\name/\pos in {
      OS/O_s/below right,
      GONE/G_1/above,
      GTWO/G_2/left,
      E/E/below right}
      {\node[joint] at (\p) {}; \node[callout,\pos=3pt] at (\p) {$\name$};}
    \node[joint] at (BONE) {};
    \node[callout,below left=3pt] at (BONE) {$B_1$};
    \node[joint,draw=orange!80!black] at (BTWO) {};
    \node[callout,below right=3pt] at (BTWO) {$B_2$};
    \node[joint,draw=teal!70!black,fill=teal!8,minimum size=7pt] at (PC) {};
    \node[callout,left=4pt,text=teal!55!black] at (PC)
      {$P_c=\mathbf{x}_c(\mathbf q,\mathbf d)$};

    % Base, crank, rocker, and distinct ternary-bar attachment offsets.
    \node[callout,rotate=-75] at ($(OS)!0.55!(GONE)+(0.18,0)$) {$\mathbf b_1$};
    \node[callout,rotate=-30] at ($(OS)!0.55!(GTWO)+(0,-0.15)$) {$\mathbf b_2$};
    \node[callout,rotate=-25,text=blue!68!black] at ($(OS)!0.50!(E)+(0,-0.14)$)
      {$r_s$};
    \node[callout,rotate=28] at ($(GONE)!0.52!(BONE)+(0,0.12)$) {$L_{r1}$};
    \node[callout,rotate=62] at ($(GTWO)!0.52!(BTWO)+(0.12,0)$) {$L_{r2}$};
    \node[callout,rotate=15,text=teal!55!black] at ($(E)!0.60!(BONE)+(0,0.16)$)
      {$\sigma_1$};
    \node[callout,rotate=15,text=orange!70!black] at ($(E)!0.56!(BTWO)+(0,-0.18)$)
      {$s_{2,k}\in[s_2^-,s_2^+]$};
    \node[callout,text=teal!55!black,right=2pt] at ($(BONE)!0.50!(PC)$)
      {$\rho_c,\psi_c$};
    \draw[-{Latex[length=2mm]},blue!68!black,thin]
      ($(OS)+(-0.72,0)$) arc[start angle=180,end angle=155,radius=0.72];
    \node[callout,text=blue!68!black] at ($(OS)+(-0.78,0.31)$) {$\phi_k$};

    % Desired path samples and a one-point distal contact.
    \draw[dashed,draw=teal!55!black,thin]
      (0.72,1.20) .. controls (0.92,1.02) and (1.10,0.77) .. (1.20,0.45);
    \foreach \p in {(0.73,1.19),(0.91,1.02),(1.08,0.78),(1.19,0.47)}
      \fill[teal!55!black] \p circle (1.1pt);
    \node[callout,text=teal!55!black,above left=1pt] at (0.73,1.19)
      {$\mathbf{x}^{\star}_{c,k}$};
    \draw[-{Latex[length=2mm]},teal!55!black,thin] (PC)--(0.76,0.35);
    \node[callout,text=teal!55!black,below left=1pt] at (0.83,0.42)
      {single distal contact};

    % Parameter key placed alongside the geometry as requested.
    \node[box,anchor=north west] at (10.0,5.65) {
      \textbf{Optimization parameters}\\[2pt]
      $\mathbf d=(\mathbf b_1,\mathbf b_2,\mathbf c_s,L_{r1},L_{r2},$\\
      $\sigma_1,s_2^-,s_2^+,\rho_c,\psi_c,\ldots)$\\
      base-anchor vectors; horn eccentricity; follower lengths; distinct
      ternary-bar offsets; slot travel; rigid contact offset.\\[4pt]
      \textbf{Operating state at sample $k$}\\[2pt]
      $\mathbf q_k$: horn, ternary-bar, and follower poses\\
      $s_{2,k}$: travel derived from those poses\\
      $\phi_k$: prescribed absolute servo angle\\[4pt]
      \textbf{Synthesis target}\\[2pt]
      $\mathbf{x}^{\star}_{c,k}$: desired one-point distal path/contact;
      finger lengths $\ell_p,\ell_m,\ell_d$ are measured inputs, not linkage
      design variables.
    };
  \end{tikzpicture}
  \caption{Reference-directed double-anchored architecture. The eccentric horn
  pin $E$ drives one ternary bar; two grounded followers meet that same bar at
  distinct offsets $B_1$ and $B_2$, with $B_1$ at the far end of its straight
  driven span. The orange $B_2$ connection shows the proposed single bounded
  pin-in-slot that supplies generic one-DOF passive motion, while $P_c$ lies on
  a rigid contact arm of that same bar.}
  \label{fig:digit-synthesis}
\end{figure}

This incidence also determines the mobility. The moving horn, ternary bar, and
two followers contribute 12 planar coordinates. If $B_1$ and $B_2$ are both
ordinary revolutes, the six revolutes contribute up to 12 independent scalar
equalities: the passive assembly is generically zero-DOF, and prescribing the
servo angle overconstrains it. Replacing exactly one bar--follower revolute by
a one-equality slot gives one passive DOF and zero DOF with the servo
prescribed. Replacing both by slots leaves one driven DOF and therefore needs
one further scalar contact, coordination, or equilibrium condition. If the
physical all-revolute assembly sweeps, then an apparent anchor is not actually
fixed, an additional body or joint is hidden, the geometry has a special rank
dependency, or compliance and clearance supply the missing motion.
The orange slot is therefore a kinematically viable synthesis choice, not a
claim that the photographed perforations are continuous sliding joints; those
holes may instead represent clamped, design-time attachment choices.

The fixed offset $\sigma_1$ belongs to the design vector and remains constant
across sampled poses. At $B_2$, a clamped perforation would likewise be a
design-time offset and an ordinary revolute. The runtime slider shown instead
adds a physical coordinate $s_{2,k}$, which the implementation eliminates from
$\mathbf q_k$ after deriving it from the body poses. Let the slot body have
local origin $\mathbf o_s$, unit axis $\mathbf u_s$, and unit normal
$\mathbf n_s$. Its world locus and eliminated-coordinate residual are
\begin{align}
  \mathbf{x}_s(s)&=\mathbf p_s+R(\theta_s)
    (\mathbf o_s+s\mathbf u_s),\qquad s^-\le s\le s^+,\\
  r_{\perp}&=(R(\theta_s)\mathbf n_s)^{\mathsf T}
    \left[\mathbf{x}_p-\mathbf p_s-R(\theta_s)\mathbf o_s\right]=0,\label{eq:slot}\\
  s&=\mathbf u_s^{\mathsf T}R(-\theta_s)
    (\mathbf{x}_p-\mathbf p_s)-\mathbf u_s^{\mathsf T}\mathbf o_s.
\end{align}
Equation~\eqref{eq:slot} removes only normal separation; the travel interval is
an inequality. Replacing a revolute by this joint therefore removes one scalar
equality and generally adds one passive mobility. That mobility must be removed
by one independent command or constraint; in Figure~\ref{fig:digit-synthesis},
the prescribed servo angle supplies that equality. With two slots, one driven
mobility would remain and would need a contact, coordination, or equilibrium
condition.

Friction is deliberately not inserted into geometric closure. In a later
quasi-static stage, a slot reaction with normal magnitude $N$ can satisfy
\begin{equation}
  |F_t|\le \mu_s N \quad\hbox{(sticking)},\qquad
  F_t=-\mu_kN\,\operatorname{sign}(\dot s) \quad\hbox{(sliding)}.
\end{equation}
The normal load and the stick--slip mode are not identifiable from positions
alone. Reported FFF-polymer friction values vary substantially with material
pair, print orientation, surface roughness, load, speed, and lubrication
\cite{przekop2020graphite,he2023tribological,norani2020abs}. Consequently the
model stores a user-supplied coefficient as metadata but does not label a
single PLA or ABS value as a universal default.

\section{Implicit design sensitivities and trajectory optimization}

Let $\mathbf d$ collect the geometry named in
Figure~\ref{fig:digit-synthesis}, and solve one converged mechanism state for
each prescribed command $\phi_k$:
\begin{equation}
  \mathbf r(\mathbf q_k,\mathbf d,\phi_k)=\mathbf 0.
  \label{eq:parametric-closure}
\end{equation}
A basic single-contact synthesis objective is
\begin{equation}
  \min_{\mathbf d}\;\Phi(\mathbf d)=
  \sum_{k=1}^{N}w_k
  \left\|\mathbf x_c(\mathbf q_k,\mathbf d)
  -\mathbf x^{\star}_{c,k}\right\|_2^2
  +\lambda_L\mathcal R_L(\mathbf d)
  +\lambda_M\mathcal R_M(\mathbf d),
  \label{eq:design-objective}
\end{equation}
subject to closure, ROM, slot-travel, manufacturability, and collision
constraints at every sample. $\mathcal R_L$ may penalize excessive total link
length, while $\mathcal R_M$ can preserve clearance from singularity through a
conditioning margin. The desired points may be sampled from a measured distal
trajectory or generated from the simplified finger model.

On a converged branch with a fixed active set, differentiating
Equation~\eqref{eq:parametric-closure} gives
\begin{equation}
  J_q S+J_d=0,\qquad
  J_q=\frac{\partial\mathbf r}{\partial\mathbf q},\quad
  J_d=\frac{\partial\mathbf r}{\partial\mathbf d},\quad
  S=\frac{\partial\mathbf q}{\partial\mathbf d}.
  \label{eq:implicit-sensitivity}
\end{equation}
The implementation estimates both Jacobians with central differences and
solves $J_qS=-J_d$ directly for square full-rank systems or by a full-column-rank
least-squares solve for tall systems. The total objective derivative is
\begin{equation}
  \frac{d\Phi}{d\mathbf d}
  =\frac{\partial\Phi}{\partial\mathbf d}
   +S^{\mathsf T}\frac{\partial\Phi}{\partial\mathbf q}.
  \label{eq:objective-gradient}
\end{equation}
This is local implicit differentiation, not automatic differentiation through
the iterative solver. An underdetermined or rank-deficient $J_q$, an
incompatible overdetermined linearization, a branch switch, a newly active ROM
or slot bound, or a stick--slip transition invalidates the ordinary derivative
and is reported rather than regularized silently.

\section{Residual equations, rank, and local mobility}

Each nonfixed planar link contributes
$\mathbf{q}_i=(x_i,y_i,\theta_i)$ to the component configuration vector. For a
revolute joint $j$ between bodies $A$ and $B$, with local attachment points
$\mathbf{a}_j$ and $\mathbf{b}_j$, the two closure residuals are
\begin{equation}
  \mathbf{r}_j(\mathbf{q})=
  \mathbf{p}_A+R(\theta_A)\mathbf{a}_j
  -\mathbf{p}_B-R(\theta_B)\mathbf{b}_j.
  \label{eq:revolute}
\end{equation}
For a world-side joint the corresponding ground point replaces the first body
term. A locked revolute adds the wrapped angular residual
\begin{equation}
  r^{\theta}_j=\ell_{\theta}
  \operatorname{wrap}(\theta_B-\theta_A-\bar\theta_j),
\end{equation}
where $\ell_{\theta}=50\,\mathrm{mm}$ converts radians to a length-like scale.
This characteristic length is comparable to the demonstrator links and avoids
combining angular and positional residuals without scaling. The prescribed
actuator residual uses the same scale.

For stacked residual vector $\mathbf{r}$, a central finite-difference Jacobian
approximates
\begin{equation}
  J(\mathbf{q})=\frac{\partial\mathbf{r}}{\partial\mathbf{q}}.
\end{equation}
Scaled elimination with named absolute and relative pivot tolerances estimates
the rank. The local mobility is
\begin{equation}
  M_{\mathrm{local}}=n_q-\operatorname{rank}J.
  \label{eq:mobility}
\end{equation}
The calculation is performed once without the actuator equality and once with
the current command prescribed. Rank, rather than raw equation count, is the
informative local diagnostic when constraints are dependent.

At a regular default pose there are six movable links and therefore
$n_q=18$. Seven revolutes contribute 14 positional residuals, and the locked
dorsal-anchor joint contributes one angular residual. The passive Jacobian has
rank 15, giving $M_{\mathrm{passive}}=3$. Adding the absolute servo-orientation
equality raises the rank to 16 and gives $M_{\mathrm{driven}}=2$. The classical
four-bar subgraph accounts for one passive coordinate and becomes zero-DOF
when driven. The two remaining coordinates belong to the middle and distal
driver joints and are resolved later by finger/contact constraints.

\section{Analytic propagation}

The solver first marks fixed links resolved and constructs the servo-driven
pose from its referenced joint. It then repeats bounded graph passes until no
operation makes progress.

\paragraph{Two-point rigid reconstruction.}
Suppose local points $\mathbf{q}_1,\mathbf{q}_2$ on one unresolved body have
known world positions $\mathbf{x}_1,\mathbf{x}_2$. After checking
$\|\mathbf{x}_2-\mathbf{x}_1\|\simeq
 \|\mathbf{q}_2-\mathbf{q}_1\|$, the pose is
\begin{align}
  \theta &={\rm atan2}(x_{2y}-x_{1y},x_{2x}-x_{1x})
           -{\rm atan2}(q_{2y}-q_{1y},q_{2x}-q_{1x}),\\
  \mathbf{p}&=\mathbf{x}_1-R(\theta)\mathbf{q}_1.
\end{align}
Coincident local points are degenerate, and incompatible local/world distances
are inconsistent. Link length is not changed by reconstruction.

\paragraph{Locked-angle propagation.}
If one body at a revolute is resolved and
$\theta_B-\theta_A=\bar\theta_j$ is locked, the other orientation follows
directly. Its translation then places its own local attachment at the known
world joint. This graph rule replaces the former special dorsal-anchor angle.

\paragraph{Topology-discovered dyads.}
Two unresolved bodies form an analytic dyad when they share a revolute joint
$X$ and each has one other revolute attached to resolved geometry. Let the
known outer joint positions be $\mathbf{c}_1,\mathbf{c}_2$. The circle radii
come from local attachment geometry, not rectangle length:
\begin{equation}
  \rho_1=\|\mathbf{q}_{X,1}-\mathbf{q}_{O,1}\|,
  \qquad
  \rho_2=\|\mathbf{q}_{X,2}-\mathbf{q}_{O,2}\|.
\end{equation}
The shared point satisfies
\begin{equation}
  \|\mathbf{x}_X-\mathbf{c}_1\|=\rho_1,
  \qquad
  \|\mathbf{x}_X-\mathbf{c}_2\|=\rho_2.
\end{equation}
The robust circle routine distinguishes two intersections, tangency, separation,
containment, coincidence, concentricity, and invalid radii. Each candidate is
reconstructed through the general two-point transform and checked against
ordinary joint ROM inequalities before acceptance.

When two candidates remain, the selected branch minimizes distance to the
preceding valid world position of joint $X$. This history is a transient map
keyed by arbitrary joint ID. If no valid snapshot exists, the current initial
joint position supplies the preference. Analytic steps retain their circle
centres, radii, candidates, and selected point for optional construction
rendering; rendering only reads this trace.

\section{Singularity and constraint diagnostics}

A tangent dyad is a branch-merging analytic singularity. Coincident circles are
degenerate and underdetermined rather than a unique closure. Independently, a
drop in the locally expected Jacobian rank marks a singular or near-singular
component. Zero mobility by itself is not singularity.

Three other conditions remain distinct. For $m$ scalar residuals and local rank
$r$:
\begin{itemize}
  \item a structural overconstraint flag identifies a subsystem with more
    required scalar equalities than available variables;
  \item $m-r>0$ identifies dependent or redundant residual rows at the sampled
    pose, which need not invalidate a consistent mechanism; and
  \item geometric inconsistency means the closure norm cannot be reduced below
    tolerance.
\end{itemize}
Diagnostics report component membership, passive and driven mobility, rank,
unresolved links, residual norm, analytic solve count, numerical-fallback use,
and explanatory messages. Disconnected or intentionally free components may
therefore be valid while still carrying useful warnings.

\section{Bounded numerical fallback}

After analytic propagation reaches a fixed point, rank analysis decides whether
an unresolved subsystem is genuinely free or expected to close. Free
coordinates preserve their finite prior values and do not enter an optimizer.
For a constrained remainder, the initial condition combines the current partial
analytic result with the preceding valid coordinates. A damped
Gauss--Newton/Levenberg--Marquardt step solves
\begin{equation}
  \left(J^{\mathsf T}J+\lambda D\right)\Delta\mathbf{q}
  =-J^{\mathsf T}\mathbf{r},
  \label{eq:damped-step}
\end{equation}
where $D$ provides diagonal scale regularization. Central differences construct
$J$; translation and angular increments have separate caps. A bounded line
search accepts only finite, residual-decreasing candidates. Ordinary joint ROM
bounds use constant-dimension active-limit residuals together with simultaneous
averaged angle projection, avoiding constraint-ID ordering as a numerical
policy. The actuator revolute instead follows the servo's absolute command
bounds.

The iteration has a finite cap, exits early below closure tolerance, and reports
failure rather than committing a nonfinite or high-residual pose. Numerical
minimization is thus a local kinematic fallback, not dynamics, branch search, or
a device for hiding mobility.

\section{Simultaneous finger and contactor solve}

The finger remains a serial proximal--middle--distal chain downstream of the
general mechanism graph. DIP flexion is coupled to PIP flexion by
$\gamma=k\beta$, with $k=0.62$. For contactor $j$, let
$\mathbf{F}_j(\alpha,\beta)$ be its point on the finger centreline and
$\mathbf{n}_{d,j}$ the segment normal directed toward positive world $Y$. Let
$W_j$ be total internal ring width. Its admissible domain and per-side
affordance are
\begin{equation}
  W_j\geq w_{f,j},\qquad a_j=\frac{W_j-w_{f,j}}{2}\geq0.
\end{equation}
The linkage centreline target is
\begin{equation}
  \mathbf{T}_j=\mathbf{F}_j+
  \left(\frac{W_j}{2}+\frac{w_{l,j}}{2}\right)\mathbf{n}_{d,j}.
\end{equation}
The specialized default solve uses
$\mathbf{z}=(\alpha,\beta,\eta_m,\eta_d)$: MCP and PIP angles plus the two
passive driver angles. With linkage contact points $\mathbf{P}_m$ and
$\mathbf{P}_d$, it minimizes
\begin{equation}
  \mathbf{r}_c(\mathbf{z})=
  \begin{bmatrix}
    \mathbf{P}_m-\mathbf{T}_m\\
    \mathbf{P}_d-\mathbf{T}_d
  \end{bmatrix}.
\end{equation}
A projected damped iteration enforces hand and driver limits. If one variable
reaches a bound, available variables continue solving. This stage consumes the
already-resolved upstream linkage and does not invoke a four-bar solver.

Every rectangular link is then tested against each phalanx capsule, except for
the link carrying the contactor assigned to that phalanx. For other pairs,
centreline segments $S_l,S_f$, widths $w_l,w_f$, and clearance $\epsilon$, the
pose requires
\begin{equation}
  \operatorname{dist}(S_l,S_f)\geq\frac{w_l+w_f}{2}+\epsilon.
\end{equation}
Ground-plane and dorsal-rail crossings are also rejected. Failure restores the
last valid mechanism, hand, mount, contactor, and joint-limit state.

\section{Gravity-only static moments}

Each phalanx is approximated as a uniform cylinder, consistent with published
index-finger and hand parameterizations
\cite{qiu2014orthopaedic,tsakonas2026handmass}. For rendered length $L_i$,
diameter $d_i$, and tissue density $\rho=1160\,\mathrm{kg\,m^{-3}}$, mass is
\begin{equation}
  m_i=\rho\pi\left(\frac{d_i}{2}\right)^2L_i.
\end{equation}
With gravity $\mathbf{g}=(0,-g)$ and joint point $\mathbf{J}_k$, the world
gravity moment from distal segments is
\begin{equation}
  \tau^{g,z}_k=\sum_{i\in\mathcal{D}(k)}
  (\mathbf{r}_{i,k}\times m_i\mathbf{g})_z,
  \qquad \mathbf{r}_{i,k}=\mathbf{C}_i-\mathbf{J}_k.
\end{equation}
An optional torsional term has form
$\tau^s_k=-K_k(q_k-q_{0,k})$, with default $K_k=0$, and the displayed holding
moment is $\tau^{hold}_k=-(\tau^g_k+\tau^s_k)$. It excludes tendon forces,
contact forces, friction, and joint reactions \cite{tsakonas2024parameter}.

\section{Interaction, construction geometry, and export}

Dragging the driven-link handle converts the direction from its arbitrary
servo attachment to the visual handle into the link's absolute angle. Other
actuator-connected handles sample the finite primary-servo range and retain the
valid pose with the nearest endpoint. A genuinely free attached link rotates
about its revolute point. This remains a bounded one-coordinate inverse control
rather than a general drag constraint.

The joint inspector exposes both graph endpoints: the reference may be world
or another link, and the target is a link. Reassigning either endpoint preserves
the current world hinge while recomputing the corresponding local attachment
coordinates, after which the ordinary graph build and validation path runs.
Self-edges and missing targets are rejected.

The construction layer retains ordinary linkage cues and consumes the latest
analytic trace. For each discovered dyad it can render both circle loci and the
selected shared-joint intersection. These values originate in the simulation
stage; Canvas rendering remains read-only.

The JSON exporter serializes a schema name and version, timestamp, units,
shared hand scale, active tab, and detached D2--D5 persistent states. Solver
diagnostics, analytic trace, and prior-joint branch history are transient and
are omitted. Angles remain radians and lengths remain millimetres.

\section{Update ordering and invariants}

The frame update is ordered as follows:
\begin{enumerate}
  \item advance or accept the absolute servo command;
  \item synchronize editable dorsal mounting geometry;
  \item build and analyze the constraint graph;
  \item run analytic propagation and, only when required, numerical fallback;
  \item solve the specialized finger/contact constraints;
  \item validate collisions and hard geometric exclusions;
  \item calculate finger statics;
  \item capture the valid finite snapshot and render it.
\end{enumerate}
Every committed revolute satisfies Equation~\eqref{eq:revolute} within named
closure tolerance and every non-actuator revolute satisfies its configured ROM
inequality. All link positions, angles, and residuals must be finite. Analytic
branches use only prior valid joint history, and a failed candidate or topology
edit cannot replace that snapshot.

\section{Remaining mathematical scope}

The graph presently models revolute planar mechanisms, bounded linear slots,
and one prescribed rotational servo. Slot friction is metadata only: there is
still no force-equilibrium or stick--slip solve. The model has no general
prismatic actuator, multiple independent actuator commands, compliance,
rigid-body dynamics, or force-balanced contact.
The numerical fallback is local and does not guarantee a global solution or
deliberate assembly-branch transition. The two finger contactors remain a
specialized downstream solve rather than arbitrary graph constraints.

The next coherent milestone is to replace the demonstrator geometry with a
calibrated reference-derived candidate, express its single distal contact and
finger coordinates in the same residual formulation, and wrap the implemented
local sensitivities in a bounded multi-pose optimizer. Force-balanced slot
reactions and experimentally calibrated friction belong in a subsequent
quasi-static stage.

\bibliographystyle{plain}
\bibliography{main}

\end{document}
